'use client'; import { useEffect, useState } from 'react'; import ThreadCard from '@/components/community/ThreadCard'; export default function Page() { const [rows, setRows] = useState([]); useEffect(() => { fetch('/api/forums/trending').then(r=>r.json()).then(j=>setRows(j.data||[])); }, []); return <div className="mx-auto max-w-4xl space-y-4 px-4 py-6"><h1 className="text-2xl font-bold text-gray-900">Trending threads</h1><div className="space-y-2">{rows.map(t=><ThreadCard key={t.id} t={t} href={`/forums/${t.specialty}/${t.id}`}/>)}</div></div>; }
