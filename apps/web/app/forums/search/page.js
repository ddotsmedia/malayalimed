'use client'; import { useState } from 'react'; export default function Page() { const [q, setQ] = useState(''); const [rows, setRows] = useState([]); async function go(e) { e.preventDefault(); const r = await fetch(`/api/forums/search?q=${encodeURIComponent(q)}`); const j = await r.json(); setRows(j.data||[]); } return <div className="mx-auto max-w-2xl space-y-4 px-4 py-6"><h1 className="text-xl font-bold text-gray-900">Search forums</h1><form onSubmit={go} className="flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search…" className="flex-1 rounded-lg border border-gray-300 px-3 py-2 text-sm"/><button className="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white">Search</button></form></div>; }
