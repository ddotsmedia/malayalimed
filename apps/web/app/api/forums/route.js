import { NextResponse } from 'next/server'; import { safeQuery } from '@mm/db'; export const dynamic = 'force-dynamic'; export async function GET() { return NextResponse.json({ data: await safeQuery('SELECT DISTINCT specialty FROM forum_threads WHERE specialty IS NOT NULL LIMIT 20'), errors: null }); } export async function POST(request) { const b = await request.json(); const res = await safeQuery('INSERT INTO forum_threads (specialty, thread_type, title, content) VALUES ($1,$2,$3,$4) RETURNING id', [b.specialty, b.thread_type || 'general', b.title, b.content]); return NextResponse.json({ data: res[0], errors: null }, { status: 201 }); }
