import { NextResponse } from 'next/server'; import { safeQuery } from '@mm/db'; export const dynamic = 'force-dynamic'; export async function GET(request) { const q = new URL(request.url).searchParams.get('q') || ''; const data = await safeQuery(`SELECT id, specialty, thread_type, title, views FROM forum_threads WHERE title ILIKE $1 OR content ILIKE $1 LIMIT 30`, [`%${q}%`]); return NextResponse.json({ data, errors: null }); }
