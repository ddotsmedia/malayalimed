import { NextResponse } from 'next/server'; import { safeQuery } from '@mm/db'; export const dynamic = 'force-dynamic'; export async function GET(request, { params }) { const { id } = await params; const data = await safeQuery('SELECT id, author_id, content, created_at FROM group_posts WHERE group_id=$1 ORDER BY created_at DESC LIMIT 50', [id]); return NextResponse.json({ data, errors: null }); }
