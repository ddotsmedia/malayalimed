import { NextResponse } from 'next/server'; import { safeQuery } from '@mm/db'; export const dynamic = 'force-dynamic'; export async function GET(request, { params }) { const { id } = await params; const data = await safeQuery('SELECT * FROM groups WHERE id=$1', [id]).then(r => r[0]); return data ? NextResponse.json({ data, errors: null }) : NextResponse.json({ errors: ['not_found'] }, { status: 404 }); }
