import { NextResponse } from 'next/server'; import { safeQuery } from '@mm/db'; export const dynamic = 'force-dynamic'; export async function GET(request, { params }) { const { specialty } = await params; const data = await safeQuery('SELECT * FROM groups WHERE lower(specialty)=lower($1) AND type=$2 LIMIT 1', [specialty, 'specialty']); return NextResponse.json({ data: data[0] || null, errors: null }); }
