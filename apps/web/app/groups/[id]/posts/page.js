'use client'; import { use, useEffect, useState } from 'react'; import PostCard from '@/components/community/PostCard'; export default function Page({ params }) { const { id } = use(params); const [posts, setPosts] = useState([]); useEffect(() => { fetch(`/api/groups/${id}/posts`).then(r=>r.json()).then(j=>setPosts(j.data||[])); }, [id]); return <div className="mx-auto max-w-2xl space-y-4 px-4 py-6"><a href={`/groups/${id}`} className="text-sm text-brand">← Group</a><h1 className="text-xl font-bold text-gray-900">Posts</h1><div className="space-y-2">{posts.map(p=><PostCard key={p.id} p={p}/>)}</div></div>; }
