'use client'; import { useState } from 'react'; export default function Page() { const [name, setName] = useState(''); const [type, setType] = useState('support'); async function submit(e) { e.preventDefault(); await fetch('/api/groups', {method:'POST', headers:{'content-type':'application/json'}, body:JSON.stringify({name, type})}); } return <div className="mx-auto max-w-md space-y-4 px-4 py-6"><h1 className="text-xl font-bold text-gray-900">Create group</h1><form onSubmit={submit} className="space-y-3 rounded-2xl border border-gray-200 bg-white p-4"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Group name *" required className="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm"/><select value={type} onChange={e=>setType(e.target.value)} className="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm"><option value="support">Support</option><option value="specialty">Specialty</option><option value="interest">Interest</option></select><button className="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white">Create</button></form></div>; }
